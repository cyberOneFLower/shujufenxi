# 复制为 .env 后修改（与 docker-compose.prod.yml 同目录）
# 镜像名与 GitHub Actions 构建推送一致：ghcr.io/<仓库 owner 小写>/arb-monitor:标签
ARB_IMAGE=ghcr.io/your-github-username/arb-monitor:latest
# 与 MySQL 数据卷里 root 一致（仅空卷首次初始化时写入；旧卷改此处不会自动改库内密码）
MYSQL_ROOT_PASSWORD=root
